# ORKIO R0.6.4 — Premium Demo Excellence & Adaptive Coordination

Audit candidate append-only over R0.6.3.

## Implemented locally

- explicit task slicing per Orion, Chris, Laura and Orkio;
- adaptive owner contracts by intent;
- one owner-only retry without repeating specialist calls;
- partial terminal state with validated contributors preserved;
- Portuguese partial/error experience;
- thread rename with tenant-scoped persistence;
- agent and mode selector fixed in the sidebar;
- truthful SSE versus HTTP JSON evidence retained;
- speaker contract v4 retained fail-closed.

## Honest limitations

- no attachment runtime;
- no DOCX/XLSX/PPTX/PDF artifact runtime;
- no WebRTC;
- no persistent execution graph;
- no live repository/database/log access for agents;
- no new migration;
- no commit, push, merge or deploy;
- exact deployed commit equivalence is not proven;
- Vite build still requires the exact repository lockfile.
